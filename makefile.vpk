VOC = /opt/voc/bin/voc
BLD=build


all:
		mkdir -p ../$(BLD)
		cd $(BLD) && \
		$(VOC) -s \
		../deps/lists/src/lDefs.Mod \
		../deps/lists/src/strutils.Mod \
		../deps/lists/src/List.Mod \
		../deps/lists/src/StringList.Mod

tests: all
		cd ../$(BLD) && \
		$(VOC) ../deps/lists/tst/TestList.Mod -m
		cd $(BLD) && \
		$(VOC) ../deps/lists/tst/TestStrutils.Mod -m
		$(BLD)/TestStrutils
		$(BLD)/TestList

clean:
		if [ -d "$(BLD)" ]; then rm -rf $(BLD); fi
